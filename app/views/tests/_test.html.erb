<tr>
  <td><%= link_to test.title, test_path(test) %></td>
  <td><%= test.level %></td>
  <td><%= test_category(test) %></td>
  <td><%= test.questions.count %></td>
  <td><%= button_to 'Start', start_test_path(test) %></td>
</tr>
